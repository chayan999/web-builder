import React from 'react';
import Nav from '../../Shared/Nav/Nav';
import MineHeader from '../MaineHeader/MineHeader';

const Header = () => {
    return (
        <div>
            <Nav></Nav>
            <MineHeader></MineHeader>
        </div>
    );
};

export default Header;